[pytest]
addopts = -ra -p no:tmpdir -p no:cacheprovider
testpaths =
    tests
    financial_analyzer/tests
norecursedirs =
    backups
    outputs
    tmp_run
    .tmp
    __pycache__
    pytest-cache-files-*
